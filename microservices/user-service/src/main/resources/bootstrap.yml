spring:
  application:
    name: user-service
  config:
    import: "configserver:${CONFIG_SERVER_URL:http://config-service:8888}" # Correct property
  cloud:
    config:
      fail-fast: true # Keep fail-fast if desired, it's a good practice
