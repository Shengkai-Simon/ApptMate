# Configurations are now managed by Spring Cloud Config Server.
# This file can be used for local overrides not suitable for config-repo,
# or profiles specific to local development if not using config server for local.
