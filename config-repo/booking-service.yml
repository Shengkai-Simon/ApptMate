server:
  port: ${SERVER_PORT:9200}

spring:
  application:
    name: ${APP_NAME:booking-service}

  datasource:
    url: ${POSTGRE_SQL:jdbc:postgresql://localhost:5432}/bookingdb
    username: user
    password: pass