server:
  port: ${SERVER_PORT:9300}

spring:
  application:
    name: ${APP_NAME:availability-service}

  datasource:
    url: ${POSTGRE_SQL:jdbc:postgresql://localhost:5432}/availabilitydb
    username: user
    password: pass